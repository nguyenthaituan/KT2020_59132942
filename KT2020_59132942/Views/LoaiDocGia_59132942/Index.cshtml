@model IEnumerable<KT2020_59132942.Models.LoaiDocGia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiDG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiDG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiDG }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiDG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiDG })
        </td>
    </tr>
}

</table>
